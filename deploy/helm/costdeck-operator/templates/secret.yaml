{{- $existingSecret := (lookup "v1" "Secret" .Release.Namespace (printf "%s-admin-credentials" (include "costdeck-operator.fullname" .))) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "costdeck-operator.fullname" . }}-admin-credentials
  labels:
    {{- include "costdeck-operator.labels" . | nindent 4 }}
type: Opaque
data:
  username: {{ "costdeck-admin" | b64enc | quote }}
  {{- if $existingSecret }}
  password: {{ index $existingSecret.data "password" }}
  {{- else }}
  password: {{ randAlphaNum 24 | b64enc | quote }}
  {{- end }}
