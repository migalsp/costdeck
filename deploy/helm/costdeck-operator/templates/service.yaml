apiVersion: v1
kind: Service
metadata:
  name: {{ include "costdeck-operator.fullname" . }}-api
  labels:
    {{- include "costdeck-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: api-ui
      protocol: TCP
      name: http
  selector:
    {{- include "costdeck-operator.selectorLabels" . | nindent 4 }}
