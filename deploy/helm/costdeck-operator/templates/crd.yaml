# Generated by 'make helm-crds' from config/crd/bases. DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: costdeckconfigs.finops.costdeck.io
spec:
  group: finops.costdeck.io
  names:
    kind: CostDeckConfig
    listKind: CostDeckConfigList
    plural: costdeckconfigs
    shortNames:
    - cdc
    singular: costdeckconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: |-
          CostDeckConfig is the Schema for the costdeckconfigs API.
          It is a singleton resource (one per namespace, conventionally named "default")
          that holds all provider and integration settings.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired configuration
            properties:
              clusterName:
                description: |-
                  ClusterName defines an optional identifier for this cluster.
                  Used in multi-cluster environments to distinguish bot commands.
                type: string
              features:
                description: Features holds core CostDeck feature toggles
                properties:
                  cloudPricingApi:
                    description: CloudPricingAPI toggles whether to use Public Cloud
                      Pricing API (e.g., AWS Pricing API) instead of heuristic math
                      calculations.
                    type: boolean
                type: object
              integrations:
                description: Integrations holds AI and messenger configurations
                properties:
                  ai:
                    description: AI model integration (coming soon)
                    properties:
                      baseUrl:
                        description: BaseURL is the optional API base URL, used primarily
                          for local or custom endpoints.
                        type: string
                      enabled:
                        description: Enabled toggles the AI integration on/off
                        type: boolean
                      model:
                        description: Model is the specific model to use (e.g. "gpt-4",
                          "claude-3-opus")
                        type: string
                      provider:
                        description: Provider is the AI service provider (e.g. "openai",
                          "anthropic", "gemini")
                        type: string
                      secretRef:
                        description: SecretRef is the name of the K8s Secret holding
                          the AI API key
                        type: string
                      skipSslVerify:
                        description: SkipSSLVerify allows skipping TLS certificate
                          verification for custom endpoints
                        type: boolean
                    type: object
                  mcp:
                    description: MCP Server configuration
                    properties:
                      enabled:
                        description: Enabled toggles the MCP server on/off
                        type: boolean
                      port:
                        default: 8083
                        description: Port is the HTTP port the MCP server will listen
                          on for SSE connections
                        type: integer
                    type: object
                  messenger:
                    description: Messenger integrations
                    properties:
                      webex:
                        description: Webex messenger integration (coming soon)
                        properties:
                          enabled:
                            description: Enabled toggles the Webex integration on/off
                            type: boolean
                          roomId:
                            description: RoomID is the Webex room/space ID for notifications
                            type: string
                          secretRef:
                            description: SecretRef is the name of the K8s Secret holding
                              the Webex bot token
                            type: string
                        type: object
                    type: object
                  victoriaMetrics:
                    description: VictoriaMetrics metrics integration
                    properties:
                      enabled:
                        description: Enabled toggles VictoriaMetrics as the metrics
                          source (instead of metrics-server)
                        type: boolean
                      endpoint:
                        description: |-
                          Endpoint is the PromQL-compatible query URL
                          e.g. "http://vmselect.monitoring.svc:8481/select/0/prometheus"
                        type: string
                      retentionDays:
                        default: 7
                        description: |-
                          RetentionDays is the lookback period for metrics queries.
                          Used by the Optimize feature to compute resource recommendations.
                        minimum: 1
                        type: integer
                      secretRef:
                        description: |-
                          SecretRef is the name of the K8s Secret holding VictoriaMetrics credentials
                          (keys: BEARER_TOKEN or USERNAME/PASSWORD)
                        type: string
                    type: object
                type: object
              providers:
                description: Providers holds cloud provider configurations
                properties:
                  aws:
                    description: AWS provider configuration
                    properties:
                      discoveryTags:
                        additionalProperties:
                          type: string
                        description: |-
                          DiscoveryTags defines tag key-value pairs used to filter AWS resources during discovery.
                          Only resources matching ALL specified tags will be discovered.
                        type: object
                      enabled:
                        description: Enabled toggles the AWS provider on/off
                        type: boolean
                      region:
                        description: Region is the default AWS region (e.g. "us-east-1")
                        pattern: ^[a-z]{2}-[a-z]+-\d$
                        type: string
                      resourceTypes:
                        description: |-
                          ResourceTypes lists which AWS resource types to discover and manage.
                          Supported values: "aurora", "ec2"
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      secretRef:
                        description: |-
                          SecretRef is the name of the K8s Secret holding AWS credentials
                          (keys: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
                        type: string
                    type: object
                  azure:
                    description: Azure provider configuration (coming soon)
                    properties:
                      enabled:
                        description: Enabled toggles the Azure provider on/off
                        type: boolean
                      secretRef:
                        description: SecretRef is the name of the K8s Secret holding
                          Azure credentials
                        type: string
                      subscriptionId:
                        description: SubscriptionID is the Azure subscription ID
                        type: string
                      tenantId:
                        description: TenantID is the Azure AD tenant ID
                        type: string
                    type: object
                  gcp:
                    description: GCP provider configuration (coming soon)
                    properties:
                      enabled:
                        description: Enabled toggles the GCP provider on/off
                        type: boolean
                      projectId:
                        description: ProjectID is the GCP project ID
                        type: string
                      secretRef:
                        description: SecretRef is the name of the K8s Secret holding
                          GCP service account JSON
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: status defines the observed state
            properties:
              aws:
                description: AWS provider status
                properties:
                  connected:
                    description: Connected indicates whether the provider credentials
                      are valid and the provider is reachable
                    type: boolean
                  discoveredResources:
                    description: DiscoveredResources is the count of resources found
                      during the last discovery
                    type: integer
                  error:
                    description: Error contains the last error message if connection
                      failed
                    type: string
                  lastChecked:
                    description: LastChecked is when connectivity was last verified
                    format: date-time
                    type: string
                required:
                - connected
                type: object
              azure:
                description: Azure provider status
                properties:
                  connected:
                    description: Connected indicates whether the provider credentials
                      are valid and the provider is reachable
                    type: boolean
                  discoveredResources:
                    description: DiscoveredResources is the count of resources found
                      during the last discovery
                    type: integer
                  error:
                    description: Error contains the last error message if connection
                      failed
                    type: string
                  lastChecked:
                    description: LastChecked is when connectivity was last verified
                    format: date-time
                    type: string
                required:
                - connected
                type: object
              conditions:
                description: Conditions represent the current state of the CostDeckConfig
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              gcp:
                description: GCP provider status
                properties:
                  connected:
                    description: Connected indicates whether the provider credentials
                      are valid and the provider is reachable
                    type: boolean
                  discoveredResources:
                    description: DiscoveredResources is the count of resources found
                      during the last discovery
                    type: integer
                  error:
                    description: Error contains the last error message if connection
                      failed
                    type: string
                  lastChecked:
                    description: LastChecked is when connectivity was last verified
                    format: date-time
                    type: string
                required:
                - connected
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: namespacefinops.finops.costdeck.io
spec:
  group: finops.costdeck.io
  names:
    kind: NamespaceFinOps
    listKind: NamespaceFinOpsList
    plural: namespacefinops
    singular: namespacefinops
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NamespaceFinOps is the Schema for the namespacefinops API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NamespaceFinOps
            properties:
              targetNamespace:
                description: TargetNamespace is the namespace this CR is tracking
                  metrics for
                type: string
            required:
            - targetNamespace
            type: object
          status:
            description: status defines the observed state of NamespaceFinOps
            properties:
              conditions:
                description: conditions represent the current state of the NamespaceFinOps
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              history:
                description: History contains the last 60 minutes of metrics (1 data
                  point per minute)
                items:
                  description: Data point for a specific minute
                  properties:
                    cpu:
                      description: Metric values for a single point in time
                      properties:
                        limits:
                          description: Total Limits
                          type: string
                        requests:
                          description: Total Requests
                          type: string
                        usage:
                          description: Total Usage
                          type: string
                      required:
                      - limits
                      - requests
                      - usage
                      type: object
                    memory:
                      description: Metric values for a single point in time
                      properties:
                        limits:
                          description: Total Limits
                          type: string
                        requests:
                          description: Total Requests
                          type: string
                        usage:
                          description: Total Usage
                          type: string
                      required:
                      - limits
                      - requests
                      - usage
                      type: object
                    timestamp:
                      format: date-time
                      type: string
                  required:
                  - cpu
                  - memory
                  - timestamp
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              insights:
                description: Insights contains informative labels about the namespace
                  (e.g. "Missing Requests")
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              lastUpdated:
                description: LastUpdated marks when the metrics were last successfully
                  polled
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: namespaceoptimizations.finops.costdeck.io
spec:
  group: finops.costdeck.io
  names:
    kind: NamespaceOptimization
    listKind: NamespaceOptimizationList
    plural: namespaceoptimizations
    singular: namespaceoptimization
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NamespaceOptimization is the Schema for the namespaceoptimizations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NamespaceOptimizationSpec defines the desired state of NamespaceOptimization
            properties:
              targetNamespace:
                description: TargetNamespace is the namespace this optimization applies
                  to
                type: string
            required:
            - targetNamespace
            type: object
          status:
            description: NamespaceOptimizationStatus defines the observed state of
              NamespaceOptimization
            properties:
              active:
                description: Active indicates if the optimization is currently applied
                type: boolean
              optimizedAt:
                description: OptimizedAt is when the optimization was last applied
                format: date-time
                type: string
              workloads:
                description: Workloads contains the list of optimized workloads and
                  their original values
                items:
                  description: WorkloadOptimization stores optimization details for
                    a specific workload
                  properties:
                    kind:
                      description: Kind of the workload
                      type: string
                    name:
                      description: Name of the workload (Deployment or StatefulSet)
                      type: string
                    optimized:
                      description: Optimized values applied
                      properties:
                        cpuLimit:
                          type: string
                        cpuRequest:
                          type: string
                        memoryLimit:
                          type: string
                        memoryRequest:
                          type: string
                      type: object
                    original:
                      description: Original values before optimization
                      properties:
                        cpuLimit:
                          type: string
                        cpuRequest:
                          type: string
                        memoryLimit:
                          type: string
                        memoryRequest:
                          type: string
                      type: object
                  required:
                  - kind
                  - name
                  - optimized
                  - original
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - active
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: scalingconfigs.finops.costdeck.io
spec:
  group: finops.costdeck.io
  names:
    kind: ScalingConfig
    listKind: ScalingConfigList
    plural: scalingconfigs
    singular: scalingconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ScalingConfig is the Schema for the scalingconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ScalingConfig
            properties:
              active:
                description: |-
                  Active is the manual override for scaling.
                  If null, the schedule is followed.
                  If true, the namespace is forced to Scale Up.
                  If false, the namespace is forced to Scale Down.
                type: boolean
              activeUntil:
                description: |-
                  ActiveUntil bounds the manual override in time. When set, Active is honoured only
                  until this timestamp; afterwards the schedule takes over again without anyone having
                  to clear the override by hand. Ignored when Active is null.
                format: date-time
                type: string
              exclusions:
                description: Exclusions lists resources that should never be scaled
                  down
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              schedules:
                description: Schedules define periodic scaling events
                items:
                  description: |-
                    ScalingSchedule defines when a namespace should be active.

                    It supports two mutually exclusive forms:

                      - Daily window (legacy, default): "days" lists the weekdays the window applies to and
                        the window runs from StartTime to EndTime. If EndTime is earlier than StartTime the
                        window is treated as overnight and spills into the following day.
                      - Continuous weekly window: set StartDay and EndDay to define a single uninterrupted
                        window such as Monday 00:00 -> Friday 23:59. "days" is ignored in this form. The
                        window may wrap through Sunday midnight (e.g. Friday 20:00 -> Monday 08:00).
                  properties:
                    days:
                      description: |-
                        Days of week the daily window applies to (0-6, 0=Sunday).
                        Required unless StartDay/EndDay are used.
                      items:
                        type: integer
                      maxItems: 7
                      type: array
                      x-kubernetes-list-type: atomic
                    endDay:
                      description: |-
                        EndDay is the weekday a continuous weekly window closes on (0-6, 0=Sunday).
                        Must be set together with StartDay. When set, Days is ignored.
                      maximum: 6
                      minimum: 0
                      type: integer
                    endTime:
                      description: EndTime in HH:MM format
                      pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                    startDay:
                      description: |-
                        StartDay is the weekday a continuous weekly window opens on (0-6, 0=Sunday).
                        Must be set together with EndDay. When set, Days is ignored.
                      maximum: 6
                      minimum: 0
                      type: integer
                    startTime:
                      description: StartTime in HH:MM format
                      pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                    timezone:
                      description: |-
                        Timezone for the schedule (e.g. "UTC", "America/New_York").
                        If empty, local operator time is used.
                      type: string
                  required:
                  - endTime
                  - startTime
                  type: object
                  x-kubernetes-validations:
                  - message: startDay and endDay must be set together to define a
                      continuous weekly window
                    rule: has(self.startDay) == has(self.endDay)
                  - message: a schedule needs either days (daily window) or startDay
                      and endDay (continuous weekly window)
                    rule: has(self.startDay) || (has(self.days) && size(self.days)
                      > 0)
                type: array
                x-kubernetes-list-type: atomic
              sequence:
                description: |-
                  Sequence defines the order of scaling resources.
                  Format: "Group/Version:Kind/Name" (e.g. "apps/v1:Deployment/my-app" or "apps/v1:Deployment/*")
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              targetNamespace:
                description: TargetNamespace is the namespace this config applies
                  to
                type: string
            required:
            - targetNamespace
            type: object
          status:
            description: status defines the observed state of ScalingConfig
            properties:
              conditions:
                description: Conditions represent the current state of the ScalingConfig
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAction:
                description: LastAction is the timestamp of the last scaling event
                format: date-time
                type: string
              originalReplicas:
                additionalProperties:
                  format: int32
                  type: integer
                description: |-
                  OriginalReplicas stores the previous replica counts for restoration
                  Key format: "Kind/Name"
                type: object
              phase:
                description: Phase is the current state of the config (ScaledUp, ScalingDown,
                  ScaledDown)
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: scalinggroups.finops.costdeck.io
spec:
  group: finops.costdeck.io
  names:
    kind: ScalingGroup
    listKind: ScalingGroupList
    plural: scalinggroups
    singular: scalinggroup
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ScalingGroup is the Schema for the scalinggroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ScalingGroup
            properties:
              active:
                description: |-
                  Active is the manual override for scaling.
                  If null, the schedule is followed.
                  If true, the group is forced to Scale Up.
                  If false, the group is forced to Scale Down.
                type: boolean
              activeUntil:
                description: |-
                  ActiveUntil bounds the manual override in time. When set, Active is honoured only
                  until this timestamp; afterwards the schedule takes over again without anyone having
                  to clear the override by hand. Ignored when Active is null.
                format: date-time
                type: string
              category:
                description: Category is the group classification (e.g. Solution,
                  Platform)
                type: string
              externalTargets:
                description: ExternalTargets allows you to manage 3rd party cloud
                  resources alongside Kubernetes resources.
                items:
                  description: ExternalTarget represents a 3rd party resource to scale
                  properties:
                    executeAfter:
                      description: ExecuteAfter specifies which namespace stage this
                        resource should scale after
                      type: string
                    identifier:
                      description: Identifier is the unique ID or ARN of the resource
                      type: string
                    name:
                      description: Name is a human-readable name for the resource
                        (e.g. EC2 "Name" tag)
                      type: string
                    provider:
                      description: Provider is the cloud provider (e.g., "aws", "gcp",
                        "azure")
                      type: string
                    region:
                      description: Region where the resource resides
                      type: string
                    status:
                      description: Status tells you if the DB is available or stopped
                        (populated at runtime by UI/Discovery API)
                      type: string
                    type:
                      description: Type of the resource (e.g., "aurora", "cloudsql")
                      type: string
                  required:
                  - identifier
                  - provider
                  - region
                  - type
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              featureFlags:
                description: FeatureFlags holds optional feature toggles for this
                  scaling group
                properties:
                  skipOnTimeout:
                    description: |-
                      SkipOnTimeout if true, namespaces that don't reach target state within TimeoutMinutes
                      will be skipped instead of blocking the entire pipeline.
                      Default: false (wait indefinitely for all services to reach target state)
                    type: boolean
                  timeoutMinutes:
                    default: 5
                    description: |-
                      TimeoutMinutes is used when SkipOnTimeout is true.
                      Namespaces not ready within this duration are skipped.
                    maximum: 30
                    minimum: 1
                    type: integer
                type: object
              namespaces:
                description: Namespaces is the list of namespaces managed by this
                  group
                items:
                  type: string
                minItems: 1
                type: array
                x-kubernetes-list-type: set
              schedules:
                description: Schedules define periodic scaling events for the group
                items:
                  description: |-
                    ScalingSchedule defines when a namespace should be active.

                    It supports two mutually exclusive forms:

                      - Daily window (legacy, default): "days" lists the weekdays the window applies to and
                        the window runs from StartTime to EndTime. If EndTime is earlier than StartTime the
                        window is treated as overnight and spills into the following day.
                      - Continuous weekly window: set StartDay and EndDay to define a single uninterrupted
                        window such as Monday 00:00 -> Friday 23:59. "days" is ignored in this form. The
                        window may wrap through Sunday midnight (e.g. Friday 20:00 -> Monday 08:00).
                  properties:
                    days:
                      description: |-
                        Days of week the daily window applies to (0-6, 0=Sunday).
                        Required unless StartDay/EndDay are used.
                      items:
                        type: integer
                      maxItems: 7
                      type: array
                      x-kubernetes-list-type: atomic
                    endDay:
                      description: |-
                        EndDay is the weekday a continuous weekly window closes on (0-6, 0=Sunday).
                        Must be set together with StartDay. When set, Days is ignored.
                      maximum: 6
                      minimum: 0
                      type: integer
                    endTime:
                      description: EndTime in HH:MM format
                      pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                    startDay:
                      description: |-
                        StartDay is the weekday a continuous weekly window opens on (0-6, 0=Sunday).
                        Must be set together with EndDay. When set, Days is ignored.
                      maximum: 6
                      minimum: 0
                      type: integer
                    startTime:
                      description: StartTime in HH:MM format
                      pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                      type: string
                    timezone:
                      description: |-
                        Timezone for the schedule (e.g. "UTC", "America/New_York").
                        If empty, local operator time is used.
                      type: string
                  required:
                  - endTime
                  - startTime
                  type: object
                  x-kubernetes-validations:
                  - message: startDay and endDay must be set together to define a
                      continuous weekly window
                    rule: has(self.startDay) == has(self.endDay)
                  - message: a schedule needs either days (daily window) or startDay
                      and endDay (continuous weekly window)
                    rule: has(self.startDay) || (has(self.days) && size(self.days)
                      > 0)
                type: array
                x-kubernetes-list-type: atomic
              sequence:
                description: |-
                  Sequence defines the order of scaling namespaces.
                  Each element can be a single namespace or multiple namespaces separated by spaces (a "stage").
                  Stages are executed sequentially, waiting for all namespaces in a stage to reach the target state.
                  Example: ["ns1", "ns2 ns3", "ns4"]
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
            required:
            - category
            - namespaces
            type: object
          status:
            description: status defines the observed state of ScalingGroup
            properties:
              conditions:
                description: Conditions represent the current state of the ScalingGroup
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAction:
                description: LastAction is the timestamp of the last scaling event
                format: date-time
                type: string
              managedCount:
                description: ManagedCount is the current number of successfully managed
                  namespaces in the group
                type: integer
              namespacesReady:
                description: NamespacesReady is the number of namespaces that have
                  reached their target state
                type: integer
              namespacesTotal:
                description: NamespacesTotal is the total number of namespaces in
                  this group
                type: integer
              originalReplicas:
                additionalProperties:
                  format: int32
                  type: integer
                description: |-
                  OriginalReplicas stores the previous replica counts for restoration
                  Key format: "Namespace/Kind/Name"
                type: object
              phase:
                description: Phase is the current state of the group (ScaledUp, ScalingDown,
                  ScaledDown)
                type: string
              readyNamespaces:
                description: ReadyNamespaces is the list of namespaces that are currently
                  at their target state
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
